# Phase 3 Acceptance Criteria

- `--open vim` เปิดไฟล์ตรง line ได้
- `--open code` เปิดไฟล์ตรง line ได้
- `--open antigravity` เปิดไฟล์ได้
- หลาย result มี prompt ให้เลือก
- `--case-sensitive` ทำงานถูกต้อง
- `--regex` ทำงานถูกต้องและ handle invalid regex
- `--json` output parse ได้จริง

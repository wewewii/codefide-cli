# Phase 2 Acceptance Criteria

- `--ext` ใช้งานได้ถูกต้อง
- `--ignore` รวมกับ default ignore ได้
- `--context` เปลี่ยนจำนวน context ได้
- binary files ถูก skip
- large files ถูก skip ตาม config
- error ไม่ทำให้ CLI crash
- test coverage ครอบคลุม scanner edge cases
